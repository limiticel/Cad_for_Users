from application import create_app

# simples, rapido e prático, simple, fast and practical
app = create_app() # inicia o app, starts the app


if __name__ == "__main__":
    app.run(debug=True)#taran!!! isso aqui inicia o servidor, this here starts the server